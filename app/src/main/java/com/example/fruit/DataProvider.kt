object DataProvider {
    fun getCourse(): Course {
        return Course(
            name = "Android Development",
            description = "Learn to build Android apps using Kotlin.",
            duration = "6 Weeks"
        )
    }
}